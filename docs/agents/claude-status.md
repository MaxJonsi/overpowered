# Claude status

No active remote `claude` branch is visible as of 2026-07-13. The only remote Claude branch, `claude-work`, currently points to the pre-v0.4.0 commit (`1cb50ec`).

Claude should replace this note with the active branch, current task, and planned file list before modifying visual-owned files.
